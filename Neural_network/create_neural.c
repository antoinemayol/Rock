#include <stdlib.h>
#include <stdio.h>
#include "math_tool.h"


#define numLayers 3
#define numInputs 2
#define num


void getarr(int num,double *a)
{
	a = malloc(num*sizeof(double));
}

int main(void)
{
	double *a = malloc(numLayers * sizeof(double));


}
